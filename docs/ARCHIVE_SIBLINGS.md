# Archived sibling folders (rename items 10 / 11 / 12 / 13)

## 10 / 11 — product folders

| Archive folder | Reason |
|---|---|
| `Frequency-Update.bak-20260809` | Frequency Manager lives in `spidervip/frequency/` |
| `SpiderVIP-Dashboard.bak-20260809` | Standalone engineering/freeze live dashboard (telnet `/proc`). Distinct from SpiderVIP Console; candidate to import later under `spidervip/freeze/` or console status views. |

## 12 / 13 — split worktrees / PR folders (20260809)

These were git worktrees of `split/*` branches. Content already exists in the monorepo via later commits; tips preserved as tags:

| Archive folder | Retired branch | Archive tag |
|---|---|---|
| `SpiderVIP-pr1-freeze.bak-20260809` | `split/freeze-forensics` | `archive/split-freeze-forensics` |
| `SpiderVIP-pr2-playbook.bak-20260809` | `split/upgrade-playbook` | `archive/split-upgrade-playbook` |
| `SpiderVIP-pr3-gservice.bak-20260809` | `split/g-service-recovery` | `archive/split-g-service-recovery` |
| `SpiderVIP-pr4-python.bak-20260809` | `split/python-toolkit` | `archive/split-python-toolkit` |

Local and remote `split/*` branches were deleted after tagging. Do not treat `.bak-*` trees as active projects.

## 3 — parent folder spelling (done)

Renamed by user to:

`G:\Spider Receiver`

(not `G:\00-Spider Receiver`). Active toolkit path:

`G:\Spider Receiver\SpiderVIP-Toolkit`

Optional cleanup: delete temporary `G:\00-Spider-AgentPark` if it remains.


